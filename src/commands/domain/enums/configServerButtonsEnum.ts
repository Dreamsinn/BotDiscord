export const enum ConfigServerButtonsEnum {
    PREFIX = 'prefix',
    ADDBLACKLIST = 'Add to blacklist',
    REMOVEBLACKLIST = 'Remove from blacklist',
    ADMINROLE = 'adminrole',
    LANGUAGE = 'language',
    CLOSE = 'configClose',
    SAVE = 'configSave',
}
